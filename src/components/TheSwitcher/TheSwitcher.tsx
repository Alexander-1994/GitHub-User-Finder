import styles from './TheSwitcher.module.scss';

interface TheSwitcherProps { }

export const TheSwitcher = ({ }: TheSwitcherProps) => (
	<div className={styles.theSwitcher}>
		TheSwitcher Component
	</div>
);
